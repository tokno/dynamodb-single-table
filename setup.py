from setuptools import setup

setup(
    name="dynamodb-single-table",
    version="0.0.0",
    install_requires=[
    ],
    extras_require={
        "develop": [
        ]
    },
    entry_points={
    }
)
